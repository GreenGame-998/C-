#include<bits/stdc++.h>
using namespace std;

int main() {
    char ch[10];        //creation
    char dh[100];       

    // cin>>ch;        // can take input in the line.
    // cout<<"printing the char: "<<ch; //same with this also, will print the char until the null char is reached, and the null is just after the last char.

    cin.getline(dh,100);
    cout<<"printing the line: "<<dh;


}